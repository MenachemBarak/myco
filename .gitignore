# myco keeps its state in %APPDATA%\.myco, never in the repository. These are
# only here so a stray local run cannot commit machine state by accident.
/registry.json
/registry.json.corrupt-*
/config.json
.copilot/
*.log
*.tmp
